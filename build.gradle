subprojects {
    apply plugin: 'java'

    group 'com.jayden'
    version '1.0-SNAPSHOT'

    sourceCompatibility = 1.8

    repositories {
        maven { url property('mavenUrl') }
        mavenCentral()
    }
}